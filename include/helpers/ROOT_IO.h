#ifndef helpers__ROOT_IO_h
#define helpers__ROOT_IO_h

#include <TCanvas.h>
#include <string>

using namespace std;

void SaveC(TCanvas *, const string &); // save root canvas in string location

#endif
